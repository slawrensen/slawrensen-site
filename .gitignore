# Local tooling caches (never deploy or commit these).
.wrangler/
node_modules/

# Personal Claude Code settings (permission grants, machine-specific).
# .claude/launch.json is intentionally tracked: it is shared project config.
.claude/settings.local.json
